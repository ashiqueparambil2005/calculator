# XAIDOR Calculator

A simple, interactive web-based calculator application built using Python and Streamlit. This application allows users to perform basic arithmetic operations with a clean user interface.

## 🚀 Features

* **User-Friendly Interface:** Two distinct columns for inputting numbers.
* **Arithmetic Operations:** Supports Addition, Subtraction, Multiplication, and Division.
* **Interactive Feedback:** Displays celebratory balloons upon successful calculation.
* **Real-time Results:** clear display of the calculation output.

## 📋 Prerequisites

Before running this application, ensure you have the following installed:

* **Python 3.x**
* **Streamlit**

## 🛠️ Installation

1.  **Clone or Download the project files.**
2.  **Install the required dependency:**
    Open your terminal or command prompt and run:
    ```bash
    pip install streamlit
    ```

## 📂 Project Structure

To run this application successfully, your directory should look like this:

```text
/project-folder
    ├── 02.py                 # The main Streamlit application (provided code)
    └── calculator_logic.py   # The logic file containing math functions